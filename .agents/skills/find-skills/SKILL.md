---
name: find-skills
description: Discover and install agent skills. Use when user asks "find a skill for X", "how do I do X", or wants to extend capabilities. Uses `npx skills` CLI.
---

When user wants to extend capabilities or find a skill: use `npx skills find [query]` to search, `npx skills add <package>` to install. Skills are modular agent extensions.

Full docs: .agents/skills/find-skills/SKILL.md
